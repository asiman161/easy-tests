{
  "engines": {
    "node": "6.9.5"
  },
  "scripts": {
    "start": "cd ./frontend && npm start",
    "build": "cd ./frontend && npm run build",
    "build:watch": "cd ./frontend && npm run build:watch",
    "build:prod": "cd ./frontend && npm run build:prod",
    "heroku-prebuild": "cd ./frontend && npm install yarn",
    "install": "cd ./frontend && node ./node_modules/.bin/yarn install"
  }
}
